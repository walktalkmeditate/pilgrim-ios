import { Logger } from '../logger';
export declare function getDatabaseInfoFromConfig(config: any, // wrangler config
name: string): {
    uuid: any;
    previewDatabaseUuid: any;
    binding: any;
    name: any;
} | null;
export declare function exportLocal(config: any, // wrangler config
name: string, output: string, tables: string[], noSchema: boolean, noData: boolean, persistencePath: string, logger: Logger): Promise<void>;
