import { Logger, LogLevel } from './logger';
import { DatabaseSettings } from '../types/config';
export declare function loadConfigFromFile(configRoot: string, configFile?: string, logLevel?: LogLevel, customLogger?: Logger): Promise<{
    path: string;
    config: DatabaseSettings;
}>;
export declare function loadWranglerConfigFromFile(configRoot: string, configFile?: string, logLevel?: LogLevel, customLogger?: Logger): Promise<{
    path: string;
    config: any;
}>;
