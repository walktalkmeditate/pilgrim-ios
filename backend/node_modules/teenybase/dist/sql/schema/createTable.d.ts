import { TableData } from '../../types/table';
export declare function createTable(data: TableData, others: TableData[], _rnd?: string): {
    sql: string;
    sqlRevert: string;
    logs: string[];
};
