import { DatabaseSettings } from "../../types/config";
export declare function generateMigrations(nextConfig: DatabaseSettings, lastConfig: Partial<DatabaseSettings> | undefined, index?: number): {
    migrations: never[];
    config: null;
    extraLogs?: undefined;
} | {
    migrations: {
        sql: string;
        sqlRevert: string;
        logs: string[];
        name: string;
    }[];
    config: DatabaseSettings;
    extraLogs: string[];
};
