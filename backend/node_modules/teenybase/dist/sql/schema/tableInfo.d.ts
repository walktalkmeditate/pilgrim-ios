import { TableFieldData } from '../../types/field';
import { $Database } from '../../worker';
import { TableData } from '../../types/table';
export interface TableInfo {
    name: string;
    fields: TableFieldData[];
    logs: string[];
}
export declare function getTableInfo(db: $Database, collections: {
    name: string;
    fields?: TableFieldData[];
}[]): Promise<(TableInfo)[]>;
export declare function getSQLiteSchema(db: $Database<any>): Promise<TableData<TableFieldData<string, string, string, string>, import("../..").TableExtensionData<string>>[]>;
