import { AlterTable, TableData } from "../../types/table";
export declare function alterTable([table, lastTable, changes]: AlterTable, others: TableData[]): {
    sql: string;
    sqlRevert: string;
    logs: string[];
};
