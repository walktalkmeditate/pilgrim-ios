import { JwtAlgorithm } from '@tsndr/cloudflare-worker-jwt';
import { JWTPayload, JWTPayloadOp } from '../types/jwt';
export declare class JWTTokenHelper {
    private secret;
    readonly issuer: string;
    private algorithm;
    allowedIssuers: string[];
    constructor(secret: () => Promise<string>, issuer?: string, algorithm?: JwtAlgorithm | string, allowedIssuers?: string[]);
    createJwtToken<T = JWTPayload>(payload: T & {
        id: string;
        sub: string;
    }, secret: string, secondsDuration: number): Promise<string>;
    decodeAuth(auth: string, secret: string, /*hasOtp: boolean,*/ onlyVerified?: boolean, issuers?: string[], payload?: JWTPayloadOp): Promise<JWTPayloadOp>;
}
