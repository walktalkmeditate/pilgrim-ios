import { AlterSettings, DatabaseSettings } from '../config';
export declare function validateSettingsChange(nextConfig: DatabaseSettings, lastConfig: Partial<DatabaseSettings> | undefined): {
    config: null;
    changes: null;
} | {
    config: DatabaseSettings;
    changes: AlterSettings;
};
