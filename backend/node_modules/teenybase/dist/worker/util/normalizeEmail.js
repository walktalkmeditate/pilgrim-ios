import { punycode1 } from './punycode';
const PLUS_ONLY = /\+.*$/;
const PLUS_AND_DOT = /\.|\+.*$/g;
const providers = {
    'gmail.com': {
        cut: PLUS_AND_DOT
    },
    'googlemail.com': {
        cut: PLUS_AND_DOT,
        alias: 'gmail.com'
    },
    'hotmail.com': {
        cut: PLUS_ONLY
    },
    'live.com': {
        cut: PLUS_AND_DOT
    },
    'outlook.com': {
        cut: PLUS_ONLY
    },
    'yahoo.com': {
        cut: PLUS_ONLY
    },
    // '*': { // for all?
    //     cut: PLUS_ONLY
    // }
};
// todo what about gmail.co.uk etc?
// todo most main providers are case sensitive for the local part, but maybe not all. Need to check
export function normalizeEmail(val) {
    val = val.trim().toLowerCase();
    const emailParts = val.split("@");
    if (emailParts.length !== 2 || !emailParts[0] || !emailParts[1]) {
        throw new Error('Invalid email address');
    }
    let [local, domain] = emailParts;
    domain = punycode1.toASCII(domain); // Normalize domain (handle internationalized domains)
    const provider = providers[domain] || providers['*'];
    if (provider) {
        if (provider.cut)
            local = local.replace(provider.cut, '');
        if (provider.alias)
            domain = provider.alias;
    }
    return `${local}@${domain}`;
}
//# sourceMappingURL=normalizeEmail.js.map