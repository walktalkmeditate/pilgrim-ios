import { $Database } from '../$Database';
import { $DBExtension } from '../$DBExtension';
import { $Env } from '../env';
export type PocketUiEnv = $Env & {
    Bindings: $Env['Bindings'] & {
        POCKET_UI_VIEWER_PASSWORD?: string;
        POCKET_UI_EDITOR_PASSWORD?: string;
    };
};
export declare class PocketUIExtension<T extends PocketUiEnv = PocketUiEnv> extends $DBExtension<T> {
    getAuthToken(): Promise<string | undefined>;
    uiVersion: string;
    baseUrl: string;
    constructor(db: $Database<T>, baseUrl?: string, uiVersion?: string);
    private loginPage;
}
