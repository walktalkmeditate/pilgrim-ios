import { $Database } from '../$Database';
import { $DBExtension } from '../$DBExtension';
import { $Env } from '../env';
export declare class OpenApiExtension<T extends $Env = $Env> extends $DBExtension<T> {
    constructor(db: $Database<T>, swagger?: boolean);
    getDoc(): import("openapi3-ts/oas31").OpenAPIObject;
    private getApiRoutes;
}
