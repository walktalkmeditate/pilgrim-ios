import { HttpRoute } from "../types/route";
import { $Database } from "./$Database";
import { $Env } from './env';
export declare abstract class $DBExtension<T extends $Env = $Env> {
    readonly db: $Database<T>;
    constructor(db: $Database<T>);
    getAuthToken?(): Promise<string | undefined>;
    setup?(version: number): Promise<void>;
    routes: HttpRoute[];
}
