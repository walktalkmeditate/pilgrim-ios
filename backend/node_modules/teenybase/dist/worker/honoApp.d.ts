import { Context, Hono } from 'hono';
import { $Env } from './env';
import { $Database } from './$Database';
export declare function teenyHono<T extends $Env = $Env>(createDb: (c: Context<T>) => Promise<$Database<T>>, app?: Hono<T>, options?: {
    logger: boolean;
    cors: boolean;
}, onRequest?: (c: Context<T>) => Promise<Response | undefined>, beforeRoute?: (c: Context<T>) => Promise<Response | undefined>): Hono<T, import("hono/types").BlankSchema, "/">;
