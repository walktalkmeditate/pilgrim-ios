export declare class SecretResolver {
    private readonly env;
    private readonly encKeyEnv?;
    constructor(env: () => any, encKeyEnv?: string | undefined);
    resolver(secret?: string, required?: boolean, message?: string): () => Promise<string>;
    resolve(secret?: string, required?: boolean, message?: string): Promise<string>;
    static DEFAULT_KEY_ENV: string;
}
