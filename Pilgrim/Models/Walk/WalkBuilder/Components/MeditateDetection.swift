import Foundation
import Combine
import CombineExt

public class MeditateDetection: WalkBuilderComponent {
    public required init(builder: WalkBuilder) {}
    public func bind(builder: WalkBuilder) {}
}
